<Window x:Class="FrontolFileAnalyzer.CompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сравнение файлов Frontol"
        Icon="Assets/app.ico"
        Width="1080"
        Height="640"
        MinWidth="600"
        MinHeight="340"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <Style x:Key="EllipsisCellTextStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="4,2" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}" />
            <Setter Property="ToolTipService.ShowDuration" Value="60000" />
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Margin="2,0,0,9"
                   FontWeight="SemiBold"
                   Text="{Binding Summary}"
                   TextWrapping="Wrap" />

        <Grid Grid.Row="1" ClipToBounds="True">
            <DataGrid ItemsSource="{Binding Rows}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeColumns="True"
                      RowHeaderWidth="0"
                      MinRowHeight="28"
                      AlternatingRowBackground="#F7F7F7"
                      GridLinesVisibility="Horizontal"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Style>
                    <Style TargetType="DataGrid">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasRows}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Style>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Состояние"
                                        Binding="{Binding Status}"
                                        Width="96"
                                        MinWidth="88"
                                        ElementStyle="{StaticResource EllipsisCellTextStyle}" />
                    <DataGridTextColumn Header="Код"
                                        Binding="{Binding Code}"
                                        Width="105"
                                        MinWidth="90"
                                        ElementStyle="{StaticResource EllipsisCellTextStyle}" />
                    <DataGridTextColumn Header="Было"
                                        Binding="{Binding OldName}"
                                        Width="*"
                                        MinWidth="150"
                                        ElementStyle="{StaticResource EllipsisCellTextStyle}" />
                    <DataGridTextColumn Header="Стало"
                                        Binding="{Binding NewName}"
                                        Width="*"
                                        MinWidth="150"
                                        ElementStyle="{StaticResource EllipsisCellTextStyle}" />
                    <DataGridTemplateColumn Header="Изменения"
                                            Width="1.25*"
                                            MinWidth="220"
                                            SortMemberPath="Changes">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Margin="4,3"
                                           MaxHeight="60"
                                           VerticalAlignment="Center"
                                           Text="{Binding Changes}"
                                           TextWrapping="Wrap"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding Changes}"
                                           ToolTipService.ShowDuration="60000" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <Border Padding="24"
                    Background="#FAFBFC"
                    BorderBrush="#D9DEE5"
                    BorderThickness="1"
                    CornerRadius="4">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasRows}" Value="False">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock HorizontalAlignment="Center"
                               FontSize="17"
                               FontWeight="SemiBold"
                               Text="Различий не найдено" />
                    <TextBlock Margin="0,5,0,0"
                               HorizontalAlignment="Center"
                               Foreground="#667085"
                               Text="Товарные строки в сравниваемых файлах совпадают."
                               TextAlignment="Center"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>

        <Button Grid.Row="2"
                MinWidth="90"
                Margin="0,10,0,0"
                Padding="12,4"
                HorizontalAlignment="Right"
                IsCancel="True"
                Content="Закрыть" />
    </Grid>
</Window>
