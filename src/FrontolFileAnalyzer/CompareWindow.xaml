<Window x:Class="FrontolFileAnalyzer.CompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сравнение файлов Frontol" Width="1180" Height="680" MinWidth="850" MinHeight="480"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Margin="2,0,0,8" FontWeight="SemiBold" Text="{Binding Summary}" />
        <DataGrid Grid.Row="1" ItemsSource="{Binding Rows}" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False" RowHeaderWidth="0"
                  AlternatingRowBackground="#F7F7F7" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Состояние" Binding="{Binding Status}" Width="100" />
                <DataGridTextColumn Header="Код" Binding="{Binding Code}" Width="110" />
                <DataGridTextColumn Header="Было" Binding="{Binding OldName}" Width="260" />
                <DataGridTextColumn Header="Стало" Binding="{Binding NewName}" Width="260" />
                <DataGridTextColumn Header="Изменения" Binding="{Binding Changes}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="2" Width="90" Margin="0,9,0,0" HorizontalAlignment="Right" IsCancel="True" Content="Закрыть" />
    </Grid>
</Window>
