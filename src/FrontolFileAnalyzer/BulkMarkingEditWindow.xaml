<Window x:Class="FrontolFileAnalyzer.BulkMarkingEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Массовая смена вида маркировки"
        Icon="Assets/app.ico"
        Width="720"
        Height="540"
        MinWidth="430"
        MinHeight="340"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        Background="#F7F8FA">
    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#2F6F9F" />
        <SolidColorBrush x:Key="TextBrush" Color="#20242A" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#68707D" />
        <SolidColorBrush x:Key="DividerBrush" Color="#E1E5EA" />
        <SolidColorBrush x:Key="WarningBrush" Color="#9A6500" />
        <SolidColorBrush x:Key="ErrorBrush" Color="#B54848" />

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="14,4" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D7DBE2" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style x:Key="PrimaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="CompactButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="Padding" Value="10,2" />
        </Style>
        <Style x:Key="SourceRowStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="RowBackground"
                                Background="Transparent"
                                BorderBrush="{StaticResource DividerBrush}"
                                BorderThickness="0,0,0,1">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter TargetName="RowBackground" Property="Background" Value="#FAFBFC" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RowBackground" Property="Background" Value="#EEF4F8" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <ScrollViewer x:Name="LayoutScrollViewer"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto"
                      PanningMode="VerticalOnly">
            <Grid Height="{Binding ActualHeight, ElementName=LayoutScrollViewer}"
                  MinHeight="440">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock FontSize="19"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextBrush}"
                       Text="Массовая смена вида маркировки"
                       TextWrapping="Wrap" />
            <TextBlock Margin="0,5,0,0"
                       Foreground="{StaticResource MutedTextBrush}"
                       TextWrapping="Wrap"
                       Text="Выберите группы товаров и укажите код, который нужно установить вместо текущего." />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel>
                <TextBlock FontSize="15"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextBrush}"
                           Text="Исходные группы" />
                <TextBlock Margin="0,3,0,0"
                           Foreground="{StaticResource MutedTextBrush}"
                           Text="{Binding SelectionSummary}"
                           TextWrapping="Wrap" />
            </StackPanel>
            <WrapPanel Grid.Row="1" Margin="0,6,0,0">
                <Button Style="{StaticResource CompactButtonStyle}"
                        Click="SelectAll_Click"
                        Content="Выбрать все" />
                <Button Margin="8,0,0,0"
                        Style="{StaticResource CompactButtonStyle}"
                        Click="ClearAll_Click"
                        Content="Снять выбор" />
            </WrapPanel>
        </Grid>

        <Border Grid.Row="2"
                Background="White"
                BorderBrush="{StaticResource DividerBrush}"
                BorderThickness="1"
                CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="34,0,10,0" Height="32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="82" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="68" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource MutedTextBrush}"
                               Text="Код" />
                    <TextBlock Grid.Column="1"
                               VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource MutedTextBrush}"
                               Text="Вид маркировки" />
                    <TextBlock Grid.Column="2"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource MutedTextBrush}"
                               Text="Товаров" />
                </Grid>

                <ListBox x:Name="SourceOptionsList"
                         Grid.Row="1"
                         BorderThickness="0"
                         Background="Transparent"
                         AlternationCount="2"
                         ItemContainerStyle="{StaticResource SourceRowStyle}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Padding="10,7"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Stretch"
                                      VerticalContentAlignment="Center"
                                      IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="82" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="68" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock VerticalAlignment="Center"
                                               FontFamily="Consolas"
                                               FontWeight="SemiBold"
                                               Text="{Binding Code}" />
                                    <TextBlock Grid.Column="1"
                                               Margin="0,0,12,0"
                                               VerticalAlignment="Center"
                                               Text="{Binding Name}"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding Name}" />
                                    <TextBlock Grid.Column="2"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Center"
                                               Foreground="{StaticResource MutedTextBrush}"
                                               Text="{Binding Count, StringFormat={}{0:N0}}" />
                                </Grid>
                            </CheckBox>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextBrush}"
                       Text="Новый код маркировки" />
            <ComboBox x:Name="TargetCodeBox"
                      Margin="0,7,0,0"
                      MinHeight="34"
                      Padding="8,4"
                      IsEditable="True"
                      IsTextSearchEnabled="True"
                      StaysOpenOnEdit="True"
                      MaxDropDownHeight="260"
                      ItemsSource="{Binding TargetOptions}"
                      TextSearch.TextPath="Code"
                      Text="{Binding TargetCodeInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <Grid MinWidth="280" Margin="2,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="82" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock VerticalAlignment="Center"
                                       FontFamily="Consolas"
                                       FontWeight="SemiBold"
                                       Text="{Binding Code}" />
                            <TextBlock Grid.Column="1"
                                       Margin="8,0,0,0"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource MutedTextBrush}"
                                       Text="{Binding Name}"
                                       TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock Margin="0,6,0,0"
                       Text="{Binding TargetHint}"
                       TextWrapping="Wrap">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsCustomTarget}" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource WarningBrush}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Margin="0,0,0,6"
                       Foreground="{StaticResource ErrorBrush}"
                       Text="{Binding ValidationMessage}"
                       TextWrapping="Wrap" />
            <WrapPanel Grid.Row="1" HorizontalAlignment="Right">
                <Button MinWidth="104"
                        IsDefault="True"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="Apply_Click"
                        Content="Применить" />
                <Button MinWidth="84"
                        Margin="8,0,0,0"
                        IsCancel="True"
                        Content="Отмена" />
            </WrapPanel>
        </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
