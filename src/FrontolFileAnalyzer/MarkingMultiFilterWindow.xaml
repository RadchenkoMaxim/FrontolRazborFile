<Window x:Class="FrontolFileAnalyzer.MarkingMultiFilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Фильтр по видам маркировки"
        Width="620"
        Height="620"
        MinWidth="480"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#F7F8FA">
    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#2F6F9F" />
        <SolidColorBrush x:Key="TextBrush" Color="#20242A" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#68707D" />

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="14,4" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D7DBE2" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#EEF1F5" />
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
            <Setter Property="BorderBrush" Value="#E1E4E9" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8,7" />
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextBrush}"
                       Text="Виды маркировки" />
            <TextBlock Margin="0,4,0,0"
                       Foreground="{StaticResource MutedTextBrush}"
                       Text="Отметьте один или несколько видов. Фильтры применяются одновременно." />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Margin="0,0,10,0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource MutedTextBrush}"
                       Text="Поиск" />
            <TextBox x:Name="SearchBox"
                     Grid.Column="1"
                     MinHeight="34"
                     Padding="9,5"
                     VerticalContentAlignment="Center"
                     TextChanged="SearchBox_TextChanged"
                     ToolTip="Поиск по коду или названию вида маркировки" />
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Click="SelectVisible_Click" Content="Выбрать показанные" />
            <Button Grid.Column="1" Margin="8,0,0,0" Click="ClearVisible_Click" Content="Снять показанные" />
            <TextBlock Grid.Column="2"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource MutedTextBrush}"
                       Text="{Binding SelectionSummary}" />
        </Grid>

        <DataGrid x:Name="OptionsGrid"
                  Grid.Row="3"
                  ItemsSource="{Binding OptionsView}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  RowHeaderWidth="0"
                  RowHeight="36"
                  AlternationCount="2"
                  AlternatingRowBackground="#F1F4F8"
                  Background="White"
                  BorderBrush="#DDE1E7"
                  BorderThickness="1"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  CanUserResizeRows="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="46" CanUserSort="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      ToolTip="Включить этот вид маркировки" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Код"
                                    Binding="{Binding Code}"
                                    IsReadOnly="True"
                                    Width="80" />
                <DataGridTextColumn Header="Вид маркировки"
                                    Binding="{Binding Name}"
                                    IsReadOnly="True"
                                    Width="*" />
                <DataGridTextColumn Header="Товаров"
                                    Binding="{Binding Count, StringFormat=N0}"
                                    IsReadOnly="True"
                                    Width="90" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center"
                       Foreground="{StaticResource MutedTextBrush}"
                       Text="Пустой выбор означает: показывать все виды" />
            <Button Grid.Column="1"
                    MinWidth="96"
                    Margin="8,0,0,0"
                    IsDefault="True"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="Apply_Click"
                    Content="Применить" />
            <Button Grid.Column="2"
                    MinWidth="88"
                    Margin="8,0,0,0"
                    IsCancel="True"
                    Content="Отмена" />
        </Grid>
    </Grid>
</Window>
