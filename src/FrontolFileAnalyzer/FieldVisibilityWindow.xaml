<Window x:Class="FrontolFileAnalyzer.FieldVisibilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройка видимости полей"
        Width="600" Height="680" MinWidth="500" MinHeight="460"
        WindowStartupLocation="CenterOwner"
        Background="#F0F0F0"
        ShowInTaskbar="False">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="8" Background="White" BorderBrush="#CFCFCF" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="210" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock FontSize="15" FontWeight="SemiBold" Text="{Binding CommandTitle}" />
                    <TextBlock Margin="0,3,0,0" Foreground="#555555" Text="Снимите флажок, чтобы скрыть поле в таблице." />
                </StackPanel>
                <TextBox Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center" TextChanged="Search_TextChanged" />
            </Grid>
        </Border>

        <ListView Grid.Row="1" Margin="0,6" ItemsSource="{Binding FieldsView}" BorderBrush="#CFCFCF">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsVisible}" Padding="4">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Width="42" FontWeight="SemiBold" Text="{Binding Number}" />
                            <TextBlock Width="280" Text="{Binding Name}" />
                            <TextBlock Foreground="#666666" Text="{Binding StateText}" />
                        </StackPanel>
                    </CheckBox>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <DockPanel Grid.Row="2">
            <StackPanel Orientation="Horizontal">
                <Button Click="SelectAll_Click" Content="Все" />
                <Button Margin="6,0,0,0" Click="SelectNone_Click" Content="Ни одного" />
                <Button Margin="6,0,0,0" Click="OnlyFilled_Click" Content="Только заполненные" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="80" IsDefault="True" Click="Ok_Click" Content="ОК" />
                <Button Width="80" Margin="6,0,0,0" IsCancel="True" Content="Отмена" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
